Install
=======

Install by `pip`:

.. code-block:: bash

   pip install ruyth-core

Can be cloned on Github:

.. code-block:: bash

   git clone https://github.com/huy746/ruyth-core.git
   cd ruyth-core
   pip install .
