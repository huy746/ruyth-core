from setuptools import setup, find_packages

setup(
    name="ruythcore",
    version="0.1.0",
    author="huy746",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
